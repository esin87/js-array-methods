const states = require('../data/states.json');
const art = require('../data/art.json');

// 1. Count how many states begin with each letter.
// { 'A': 4, 'C': 3, 'D': 1, etc. }

// 2. Create a new array of all the style titles from the art data. Do not include repeating styles.

// 3. Create a new array of objects with works by Auguste Rodin. 