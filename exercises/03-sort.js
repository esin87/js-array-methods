const states = require('../data/states.json');
const art = require('../data/art.json');

// 1. Sort the states array in descending order of capital names (Z - A).

// 2. Sort the states array in ascending order of latitude.

// 3. Sort the art array in ascending order of creation (use date_start).
